import { Box } from '@mui/material';
import styled from 'styled-components';

export const UserListingContainer = styled(Box)`
    display: flex;
    min-height: 80vh;
    width: 100%;
    justify-content: center;
    align-items: center;
    flex-direction: column;
`;